import time
# import asyncio
async def process_payment(method: str, amount: float, details: dict):
    # Replace with actual payment gateway integration
    return {"status": "success", "transaction_id": f"txn_{int(time.time())}"}